services:
  - type: web
    name: kintaro-mooncake
    runtime: static
    staticPublishPath: .
    buildCommand: ""
    headers:
      - path: /products.json
        name: Cache-Control
        value: no-cache
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
